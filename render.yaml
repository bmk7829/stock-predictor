services:
  - type: web
    name: stock-predictor-backend
    env: python
    repo: https://github.com/bmk7829/stock-predictor
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
